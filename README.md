# IRREVERSO OS — Fixed Starter
This zip is meant to be copied **into** a Next.js App Router project created by create-next-app.

Important:
- Folder MUST be `app/core` (lowercase). If you have `app/CORE`, rename it.

Run:
npm i three @react-three/fiber @react-three/drei zustand framer-motion
npm run dev

Routes:
- / (porta)
- /core (sistema)
- /core/archive, /core/summary, /core/future-news (placeholders)
